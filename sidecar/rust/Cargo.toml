[package]
name = "sentinel-pii-scanner"
version = "0.1.0"
edition = "2021"

[lib]
name = "sentinel_pii_scanner"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
regex = "1"
