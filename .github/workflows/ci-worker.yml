name: CI — Approval Worker

on:
  pull_request:
    paths:
      - "approval-worker/**"

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: approval-worker
        run: pip install ".[dev]"

      - name: Lint
        working-directory: approval-worker
        run: ruff check src/ tests/

      - name: Test
        working-directory: approval-worker
        run: python -m pytest tests/ -v
